name: Linting and Validation Checks Customised

on:
  workflow_dispatch

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-node@v2.1.0

      - name: build
        run: npm run build

      - name: lint
        run: npm run lint

      - name: lintRooms
        run: npm run lint-rooms

      - name: typecheck
        run: npm run typecheck
